import { Box, Typography } from '@mui/material';
import React from 'react';

export function Settings() {
  return (
    <Box>
      <Typography variant="h4">Asetukset</Typography>
      <Typography variant="body1">Tässä on asetukset</Typography>
    </Box>
  );
}
